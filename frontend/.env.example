# Frontend Next.js — Publicador de Metadados
# Base da API do backend Express. Vazio/omitido => same-origin "/api".
# Em dev, aponte para o backend (porta padrão 3340):
NEXT_PUBLIC_API_BASE=http://localhost:3340/api
